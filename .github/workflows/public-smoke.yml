name: Public Smoke
on:
  workflow_dispatch:
jobs:
  ping:
    runs-on: [self-hosted, linux, x64, vira-probe]
    steps:
      - name: ping
        run: echo public-self-hosted-ok && uname -a && whoami && df -h / | tail -1
